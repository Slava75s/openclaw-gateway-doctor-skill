# 🛠️ OpenClaw Gateway Doctor Skill

Это интеллектуальное расширение (skill) для OpenClaw, предназначенное для глубокой диагностики, лечения и профилактики проблем со шлюзами (Gateways) на Linux (systemd).

## 🎯 Миссия: "Накопительное Умение"
Данный скилл спроектирован как **эволюционирующий инструмент**. Это не просто скрипт для одной ошибки, а живая база знаний.

### Как это работает:
1.  **Диагностика**: Скилл использует `openclaw doctor` и системные инструменты (systemctl, pgrep) для поиска аномалий.
2.  **Накопление опыта**: При обнаружении новой, ранее неизвестной проблемы, решение записывается в папку `references/`.
3.  **Обучение**: С каждой новой записью в `references/`, ИИ-агент становится умнее и применяет этот опыт для всех последующих случаев.

## 🚀 Основные возможности
*   **Восстановление токенов**: Автоматическая синхронизация `OPENCLAW_GATEWAY_TOKEN` между конфигом и сервисом.
*   **Исправление точек входа**: Лечение несоответствий `dist/entry.js` vs `dist/index.js`.
*   **Диагностика systemd**: Проверка статусов, логов и переменных окружения работающих процессов.
*   **Force-ремонт**: Переустановка шлюза с сохранением параметров при критических сбоях.

## 📁 Структура репозитория
*   `SKILL.md` — Главный манифест с процедурами (Core Logic).
*   `references/` — **Золотой фонд знаний**. Здесь хранятся технические заметки о багах, специфичных для разных версий Linux или OpenClaw.

## 🛠️ Инструкции по дополнению (Для разработчика)
Чтобы скилл стал по-настоящему универсальным, при каждом "лечении" нового бага:
1. Создай файл в `references/bag-name.md`.
2. Опиши симптомы и решение.
3. Добавь ссылку на этот файл в основной `SKILL.md`.

---
*Создано и поддерживается с помощью Gemini CLI.*
